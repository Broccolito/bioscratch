---
---

# Empty front matter

A bare `---` / `---` block is valid (Jekyll uses it to force processing). The
banner should show its empty-state hint instead of breaking, and the block must
still round-trip.
