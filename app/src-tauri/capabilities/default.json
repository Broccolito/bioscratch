{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "bioscratch-*"],
  "permissions": [
    "core:default",
    "fs:default",
    "fs:allow-read-file",
    "core:webview:allow-create-webview-window",
    "core:webview:allow-print"
  ]
}
